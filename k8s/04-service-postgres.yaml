apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    app: postgres
    lab: lab2
spec:
  selector:
    app: postgres   # Лейбл должен совпадать с подом PostgreSQL
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP   # Доступен только внутри кластера